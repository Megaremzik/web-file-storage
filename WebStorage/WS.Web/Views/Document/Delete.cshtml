@model WS.Business.ViewModels.DocumentView
<div id="deleteModal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"></button>
                <h3 class="modal-title"></h3>
            </div>
            <div class="modal-body">
                <h4 id="deleteMessege"></h4>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal"></button>
                <button type="button" class="btn btn-success" onclick="DeleteTask()"></button>
            </div>
        </div>
    </div>
</div>    

@*@{
        ViewBag.Title = "Удаление";
    }
@Html.Hidden("id", Model.Id)
    <div>
        <dl class="dl-horizontal">
            <dt>Название</dt>
            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>Расширение</dt>
            <dd>
                @Html.DisplayFor(model => model.Extention)
            </dd>

            <dt>Размер</dt>
            <dd>
                @Html.DisplayFor(model => model.Size)
            </dd>
        </dl>

        <div>
            <form asp-controller="Document" asp-action="Delete" method="post">
                <div class="form-group">
                    <input type="submit" class="btn btn-default" value="Удалить" />
                </div>
            </form>
        </div>
    </div>*@
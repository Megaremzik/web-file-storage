@model WS.Business.ViewModels.UserDocumentView

    @{
        ViewBag.Title = "Удаление";
    }
    <div>
        <dl class="dl-horizontal">
            <dt>Название</dt>
            <dd>
                @Html.DisplayFor(model => model.UserId)
                @Html.DisplayFor(model => model.UserId)
            </dd>

            <dt>Производитель</dt>
            <dd>
                @Html.DisplayFor(model => model.Link)
            </dd>

            <dt>Цена</dt>
            <dd>
                @Html.DisplayFor(model => model.DocumentId)
            </dd>
        </dl>

        <div>
            <form asp-controller="UserDocument" asp-action="Delete" asp-route-id1="@Model.UserId" asp-route-id2="@Model.DocumentId" method="post">
                <div class="form-group">
                    <input type="submit" class="btn btn-default" value="Удалить" />
                </div>
            </form>
        </div>
    </div>

@model WS.Business.ViewModels.DocumentLinkView
@{
    ViewBag.Title = Model.Id;
}
<h2>Id @Model.Id</h2>
<div>
    <dl class="dl-horizontal">
        <dt>User_id</dt>
        <dd>@Model.Id</dd>

        <dt>Name</dt>
        <dd>@Model.Link</dd>

        <dt>Date_change</dt>
        <dd>@Model.IsEditable</dd>

       
    </dl>
</div>
